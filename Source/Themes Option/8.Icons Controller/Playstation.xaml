<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <!-- Définition du style pour les boutons du menu inférieur -->
    <Style x:Key="ButtonBottomMenu" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- ICON: native Playnite InputHint first, Tag fallback second -->
                            <Grid Width="40"
                                  Height="40"
                                  Margin="0,0,5,0">

                                <!-- InputHint follows Playnite's X/A swap. -->
                                <ContentControl x:Name="InputHintIcon"
                                                Width="40"
                                                Height="40"
                                                Focusable="False"
                                                ContentTemplate="{TemplateBinding InputHint}" />

                                <!-- Fallback for your custom Tag system -->
                                <Image x:Name="IconImg"
                                       Width="40"
                                       Height="40"
                                       RenderOptions.BitmapScalingMode="Fant"
                                       Source="{DynamicResource ImageButtonA-PS}"
                                       Visibility="Collapsed" />
                            </Grid>

                            <!-- TEXT -->
                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconLightBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- InputHint first, Tag fallback; Playnite swap still works. -->
                        <Trigger Property="InputHint" Value="{x:Null}">
                            <Setter TargetName="InputHintIcon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IconImg" Property="Visibility" Value="Visible" />
                        </Trigger>


                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonA-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonB-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonX-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonY-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonStart-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonBack-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonLS-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonRS-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonDpad-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="L1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonL1-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="R1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonR1-PS}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Play / Details fallback tags. -->
            <Trigger Property="Tag" Value="PlayAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </Trigger>
            <Trigger Property="Tag" Value="DetailsAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="PlayAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="DetailsAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </MultiDataTrigger>

            <!-- Semantic tags for Confirm / Cancel. -->
            <Trigger Property="Tag" Value="ConfirmAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>
            <Trigger Property="Tag" Value="CancelAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </Trigger>

            <!-- Native A/B swap with Tag fallback. -->
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="A" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="B" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="ConfirmAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="CancelAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>
        </Style.Triggers>

    </Style>

    <Style x:Key="ButtonBottomMenuDark" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- ICON: native Playnite InputHint first, Tag fallback second -->
                            <Grid Width="40"
                                  Height="40"
                                  Margin="0,0,5,0">

                                <!-- InputHint follows Playnite's X/A swap. -->
                                <ContentControl x:Name="InputHintIcon"
                                                Width="40"
                                                Height="40"
                                                Focusable="False"
                                                ContentTemplate="{TemplateBinding InputHint}" />

                                <!-- Fallback for your custom Tag system -->
                                <Image x:Name="IconImg"
                                       Width="40"
                                       Height="40"
                                       RenderOptions.BitmapScalingMode="Fant"
                                       Source="{DynamicResource ImageButtonA-PS}"
                                       Visibility="Collapsed" />
                            </Grid>

                            <!-- TEXT -->
                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- InputHint first, Tag fallback; Playnite swap still works. -->
                        <Trigger Property="InputHint" Value="{x:Null}">
                            <Setter TargetName="InputHintIcon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IconImg" Property="Visibility" Value="Visible" />
                        </Trigger>


                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonA-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonB-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonX-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonY-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonStart-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonBack-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonLS-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonRS-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonDpad-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="L1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonL1-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="R1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonR1-PS}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Play / Details fallback tags. -->
            <Trigger Property="Tag" Value="PlayAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </Trigger>
            <Trigger Property="Tag" Value="DetailsAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="PlayAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="DetailsAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </MultiDataTrigger>

            <!-- Semantic tags for Confirm / Cancel. -->
            <Trigger Property="Tag" Value="ConfirmAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>
            <Trigger Property="Tag" Value="CancelAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </Trigger>

            <!-- Native A/B swap with Tag fallback. -->
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="A" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="B" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="ConfirmAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="CancelAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>
        </Style.Triggers>

    </Style>

    <Style x:Key="ButtonBottomMenuShoulders" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,4,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonL1-PS}" />

                            <TextBlock Text="/"
                                       Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       Margin="0,0,4,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource ButtonIconLightBrush}" />

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,8,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonR1-PS}" />

                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconLightBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ButtonBottomMenuShouldersDark" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,4,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonL1-PS}" />

                            <TextBlock Text="/"
                                       Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       Margin="0,0,4,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}" />

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,8,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonR1-PS}" />

                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <!-- Dual button: main action + hold action under it -->
    <Style x:Key="ButtonBottomMenuDual" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="18,0,18,0">

                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- ICON: native Playnite InputHint first, Tag fallback second -->
                            <Grid Width="40"
                                  Height="40"
                                  Margin="0,0,8,0">

                                <!-- Playnite/custom style can set InputHint dynamically -->
                                <ContentControl x:Name="InputHintIcon"
                                                Width="40"
                                                Height="40"
                                                Focusable="False"
                                                ContentTemplate="{TemplateBinding InputHint}" />

                                <!-- Fallback for your custom Tag system -->
                                <Image x:Name="IconImg"
                                       Width="40"
                                       Height="40"
                                       RenderOptions.BitmapScalingMode="Fant"
                                       Source="{DynamicResource ImageButtonY-PS}"
                                       Visibility="Collapsed" />
                            </Grid>

                            <!-- Texts -->
                            <StackPanel Orientation="Vertical"
                                        VerticalAlignment="Center">

                                <!-- Main action -->
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                           FontSize="18"
                                           Foreground="{DynamicResource ButtonIconLightBrush}"
                                           Text="{TemplateBinding Content}" />

                                <!-- Long press action -->
                                <StackPanel Orientation="Horizontal"
                                            Margin="0,1,0,0"
                                            Opacity="0.65">

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text="{DynamicResource LOCHold}" />

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text=": " />

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text="{TemplateBinding CommandParameter}" />

                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- InputHint first, Tag fallback; Playnite swap still works. -->
                        <Trigger Property="InputHint" Value="{x:Null}">
                            <Setter TargetName="InputHintIcon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IconImg" Property="Visibility" Value="Visible" />
                        </Trigger>


                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonA-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonB-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonX-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonY-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonStart-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonBack-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonLS-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonRS-PS}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonDpad-PS}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Play / Details fallback tags. -->
            <Trigger Property="Tag" Value="PlayAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </Trigger>
            <Trigger Property="Tag" Value="DetailsAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="PlayAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="DetailsAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </MultiDataTrigger>

            <!-- Semantic tags for Confirm / Cancel. -->
            <Trigger Property="Tag" Value="ConfirmAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>
            <Trigger Property="Tag" Value="CancelAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </Trigger>

            <!-- Native A/B swap with Tag fallback. -->
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="A" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="B" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="ConfirmAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="CancelAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>
        </Style.Triggers>

    </Style>


    <!-- Use Aniki images for native Playnite prompts. -->
    <DataTemplate x:Key="ButtonPromptA">
        <Image MaxWidth="40" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonA-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptB">
        <Image MaxWidth="40" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonB-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptX">
        <Image MaxWidth="40" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonX-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptY">
        <Image MaxWidth="40" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonY-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptStart">
        <Image MaxWidth="40" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonStart-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptBack">
        <Image MaxWidth="40" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonBack-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptLS">
        <Image MaxWidth="40" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonLS-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptRS">
        <Image MaxWidth="40" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonRS-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptLB">
        <Image MaxWidth="46" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonL1-PS}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptRB">
        <Image MaxWidth="46" MaxHeight="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonR1-PS}" />
    </DataTemplate>

    <BitmapImage x:Key="ImageButtonA-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-A.png'}" />
    <BitmapImage x:Key="ImageButtonB-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-B.png'}" />
    <BitmapImage x:Key="ImageButtonX-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-X.png'}" />
    <BitmapImage x:Key="ImageButtonY-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-Y.png'}" />
    <BitmapImage x:Key="ImageButtonStart-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-Start.png'}" />
    <BitmapImage x:Key="ImageButtonBack-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-Back.png'}" />
    <BitmapImage x:Key="ImageButtonLS-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-LS.png'}" />
    <BitmapImage x:Key="ImageButtonRS-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-RS.png'}" />
    <BitmapImage x:Key="ImageButtonDpad-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-Dpad.png'}" />
    <BitmapImage x:Key="ImageButtonL1-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-LeftShoulder.png'}" />
    <BitmapImage x:Key="ImageButtonR1-PS" UriSource="{ThemeFile 'Images/Buttons/Controller/PlayStation/Button-RightShoulder.png'}" />

</ResourceDictionary>
