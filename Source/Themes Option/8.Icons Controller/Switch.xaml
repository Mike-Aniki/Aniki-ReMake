<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <!-- Définition du style pour les boutons du menu inférieur -->
    <Style x:Key="ButtonBottomMenu" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- ICON (default = A) -->
                            <Image x:Name="IconImg"
                                   Width="40"
                                   Height="40"
                                   Margin="0,0,5,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonA-Switch}" />

                            <!-- TEXT -->
                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconLightBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonA-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonB-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonX-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonY-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonStart-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonBack-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonLS-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonRS-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonDpad-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="L1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonL1-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="R1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonR1-Switch}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ButtonBottomMenuDark" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- ICON (default = A) -->
                            <Image x:Name="IconImg"
                                   Width="40"
                                   Height="40"
                                   Margin="0,0,5,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonA-Switch}" />

                            <!-- TEXT -->
                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonA-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonB-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonX-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonY-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonStart-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonBack-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonLS-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonRS-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonDpad-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="L1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonL1-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="R1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonR1-Switch}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ButtonBottomMenuShoulders" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,4,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonL1-Switch}" />

                            <TextBlock Text="/"
                                       Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       Margin="0,0,4,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource ButtonIconLightBrush}" />

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,8,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonR1-Switch}" />

                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconLightBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ButtonBottomMenuShouldersDark" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,4,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonL1-Switch}" />

                            <TextBlock Text="/"
                                       Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       Margin="0,0,4,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}" />

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,8,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonR1-Switch}" />

                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <!-- Dual button: main action + hold action under it -->
    <Style x:Key="ButtonBottomMenuDual" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="18,0,18,0">

                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- Button icon -->
                            <Image x:Name="IconImg"
                                   Width="40"
                                   Height="40"
                                   Margin="0,0,8,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonY-Switch}" />

                            <!-- Texts -->
                            <StackPanel Orientation="Vertical"
                                        VerticalAlignment="Center">

                                <!-- Main action -->
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                           FontSize="18"
                                           Foreground="{DynamicResource ButtonIconLightBrush}"
                                           Text="{TemplateBinding Content}" />

                                <!-- Long press action -->
                                <StackPanel Orientation="Horizontal"
                                            Margin="0,1,0,0"
                                            Opacity="0.65">

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text="{DynamicResource LOCHold}" />

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text=": " />

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text="{TemplateBinding CommandParameter}" />

                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonA-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonB-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonX-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonY-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonStart-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonBack-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonLS-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonRS-Switch}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonDpad-Switch}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <BitmapImage x:Key="ImageButtonA-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-A.png'}" />
    <BitmapImage x:Key="ImageButtonB-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-B.png'}" />
    <BitmapImage x:Key="ImageButtonX-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-X.png'}" />
    <BitmapImage x:Key="ImageButtonY-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-Y.png'}" />
    <BitmapImage x:Key="ImageButtonStart-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-Start.png'}" />
    <BitmapImage x:Key="ImageButtonBack-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-Back.png'}" />
    <BitmapImage x:Key="ImageButtonLS-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-LS.png'}" />
    <BitmapImage x:Key="ImageButtonRS-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-RS.png'}" />
    <BitmapImage x:Key="ImageButtonDpad-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-Dpad.png'}" />
    <BitmapImage x:Key="ImageButtonL1-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-LeftShoulder.png'}" />
    <BitmapImage x:Key="ImageButtonR1-Switch" UriSource="{ThemeFile 'Images/Buttons/Controller/Switch/Button-RightShoulder.png'}" />

</ResourceDictionary>
