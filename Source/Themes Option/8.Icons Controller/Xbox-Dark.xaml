<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <!-- Définition du style pour les boutons du menu inférieur -->
    <Style x:Key="ButtonBottomMenu" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- ICON: native Playnite InputHint first, Tag fallback second -->
                            <Grid Width="40"
                                  Height="40"
                                  Margin="0,0,5,0">

                                <!-- Playnite changes InputHint automatically when Swap Play/Details is enabled -->
                                <ContentControl x:Name="InputHintIcon"
                                                Width="40"
                                                Height="40"
                                                Focusable="False"
                                                ContentTemplate="{TemplateBinding InputHint}" />

                                <!-- Fallback for your custom Tag system -->
                                <Image x:Name="IconImg"
                                       Width="40"
                                       Height="40"
                                       RenderOptions.BitmapScalingMode="Fant"
                                       Source="{DynamicResource ImageButtonA-XboxDark}"
                                       Visibility="Collapsed" />
                            </Grid>

                            <!-- TEXT -->
                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconLightBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- If InputHint is null, use the old Tag-based image system.
                        If InputHint is set, it wins. This lets Playnite's native swap update icons. -->
                        <Trigger Property="InputHint" Value="{x:Null}">
                            <Setter TargetName="InputHintIcon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IconImg" Property="Visibility" Value="Visible" />
                        </Trigger>


                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonA-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonB-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonX-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonY-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonStart-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonBack-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonLS-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonRS-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonDpad-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="L1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonL1-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="R1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonR1-XboxDark}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Semantic tags for Play / Details. Useful if the button is not managed by Playnite code. -->
            <Trigger Property="Tag" Value="PlayAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </Trigger>
            <Trigger Property="Tag" Value="DetailsAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="PlayAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="DetailsAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </MultiDataTrigger>

            <!-- Semantic tags for Confirm / Cancel. -->
            <Trigger Property="Tag" Value="ConfirmAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>
            <Trigger Property="Tag" Value="CancelAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </Trigger>

            <!-- Native A/B swap. Also works for simple Tag="A" / Tag="B" buttons. -->
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="A" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="B" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="ConfirmAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="CancelAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>
        </Style.Triggers>

    </Style>

    <Style x:Key="ButtonBottomMenuDark" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- ICON: native Playnite InputHint first, Tag fallback second -->
                            <Grid Width="40"
                                  Height="40"
                                  Margin="0,0,5,0">

                                <!-- Playnite changes InputHint automatically when Swap Play/Details is enabled -->
                                <ContentControl x:Name="InputHintIcon"
                                                Width="40"
                                                Height="40"
                                                Focusable="False"
                                                ContentTemplate="{TemplateBinding InputHint}" />

                                <!-- Fallback for your custom Tag system -->
                                <Image x:Name="IconImg"
                                       Width="40"
                                       Height="40"
                                       RenderOptions.BitmapScalingMode="Fant"
                                       Source="{DynamicResource ImageButtonA-XboxDark}"
                                       Visibility="Collapsed" />
                            </Grid>

                            <!-- TEXT -->
                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- If InputHint is null, use the old Tag-based image system.
                        If InputHint is set, it wins. This lets Playnite's native swap update icons. -->
                        <Trigger Property="InputHint" Value="{x:Null}">
                            <Setter TargetName="InputHintIcon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IconImg" Property="Visibility" Value="Visible" />
                        </Trigger>


                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonA-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonB-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonX-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonY-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonStart-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonBack-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonLS-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonRS-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonDpad-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="L1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonL1-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="R1">
                            <Setter TargetName="IconImg" Property="Source"
                                    Value="{DynamicResource ImageButtonR1-XboxDark}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Semantic tags for Play / Details. Useful if the button is not managed by Playnite code. -->
            <Trigger Property="Tag" Value="PlayAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </Trigger>
            <Trigger Property="Tag" Value="DetailsAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="PlayAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="DetailsAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </MultiDataTrigger>

            <!-- Semantic tags for Confirm / Cancel. -->
            <Trigger Property="Tag" Value="ConfirmAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>
            <Trigger Property="Tag" Value="CancelAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </Trigger>

            <!-- Native A/B swap. Also works for simple Tag="A" / Tag="B" buttons. -->
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="A" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="B" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="ConfirmAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="CancelAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>
        </Style.Triggers>

    </Style>

    <Style x:Key="ButtonBottomMenuShoulders" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,4,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonL1-XboxDark}" />

                            <TextBlock Text="/"
                                       Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       Margin="0,0,4,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource ButtonIconLightBrush}" />

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,8,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonR1-XboxDark}" />

                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconLightBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ButtonBottomMenuShouldersDark" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="20,0,20,0">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,4,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonL1-XboxDark}" />

                            <TextBlock Text="/"
                                       Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       Margin="0,0,4,0"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}" />

                            <Image Width="46"
                                   Height="40"
                                   Margin="0,0,8,0"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{DynamicResource ImageButtonR1-XboxDark}" />

                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="{DynamicResource ButtonIconDarkBrush}">
                                <ContentPresenter />
                            </TextBlock>

                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <!-- Dual button: main action + hold action under it -->
    <Style x:Key="ButtonBottomMenuDual" TargetType="ButtonEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonEx">
                    <Border Background="{TemplateBinding Background}"
                            Margin="18,0,18,0">

                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <!-- ICON: native Playnite InputHint first, Tag fallback second -->
                            <Grid Width="40"
                                  Height="40"
                                  Margin="0,0,8,0">

                                <!-- Playnite/custom style can set InputHint dynamically -->
                                <ContentControl x:Name="InputHintIcon"
                                                Width="40"
                                                Height="40"
                                                Focusable="False"
                                                ContentTemplate="{TemplateBinding InputHint}" />

                                <!-- Fallback for your custom Tag system -->
                                <Image x:Name="IconImg"
                                       Width="40"
                                       Height="40"
                                       RenderOptions.BitmapScalingMode="Fant"
                                       Source="{DynamicResource ImageButtonY-XboxDark}"
                                       Visibility="Collapsed" />
                            </Grid>

                            <!-- Texts -->
                            <StackPanel Orientation="Vertical"
                                        VerticalAlignment="Center">

                                <!-- Main action -->
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                           FontSize="18"
                                           Foreground="{DynamicResource ButtonIconLightBrush}"
                                           Text="{TemplateBinding Content}" />

                                <!-- Long press action -->
                                <StackPanel Orientation="Horizontal"
                                            Margin="0,1,0,0"
                                            Opacity="0.65">

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text="{DynamicResource LOCHold}" />

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text=": " />

                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}"
                                               FontSize="14"
                                               Foreground="{DynamicResource ButtonIconLightBrush}"
                                               Text="{TemplateBinding CommandParameter}" />

                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- If InputHint is null, use the old Tag-based image system.
                        If InputHint is set, it wins. This lets Playnite's native swap update icons. -->
                        <Trigger Property="InputHint" Value="{x:Null}">
                            <Setter TargetName="InputHintIcon" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IconImg" Property="Visibility" Value="Visible" />
                        </Trigger>


                        <Trigger Property="Tag" Value="A">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonA-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="B">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonB-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="X">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonX-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Y">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonY-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Start">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonStart-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Back">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonBack-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="LS">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonLS-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="RS">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonRS-XboxDark}" />
                        </Trigger>

                        <Trigger Property="Tag" Value="Dpad">
                            <Setter TargetName="IconImg" Property="Source" Value="{DynamicResource ImageButtonDpad-XboxDark}" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <!-- Semantic tags for Play / Details. Useful if the button is not managed by Playnite code. -->
            <Trigger Property="Tag" Value="PlayAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </Trigger>
            <Trigger Property="Tag" Value="DetailsAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="PlayAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="DetailsAction" />
                    <Condition Binding="{Settings Fullscreen.SwapStartDetailsAction}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptX}" />
            </MultiDataTrigger>

            <!-- Semantic tags for Confirm / Cancel. -->
            <Trigger Property="Tag" Value="ConfirmAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </Trigger>
            <Trigger Property="Tag" Value="CancelAction">
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </Trigger>

            <!-- Native A/B swap. Also works for simple Tag="A" / Tag="B" buttons. -->
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="A" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="B" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="ConfirmAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptB}" />
            </MultiDataTrigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="CancelAction" />
                    <Condition Binding="{Settings Fullscreen.SwapConfirmCancelButtons}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="InputHint" Value="{DynamicResource ButtonPromptA}" />
            </MultiDataTrigger>
        </Style.Triggers>

    </Style>


    <!-- Native Playnite prompt resources overridden with Aniki custom images.
    Playnite sets ButtonEx.InputHint to these resources and updates them when X/A is swapped. -->
    <DataTemplate x:Key="ButtonPromptA">
        <Image Width="40" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonA-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptB">
        <Image Width="40" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonB-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptX">
        <Image Width="40" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonX-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptY">
        <Image Width="40" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonY-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptStart">
        <Image Width="40" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonStart-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptBack">
        <Image Width="40" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonBack-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptLS">
        <Image Width="40" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonLS-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptRS">
        <Image Width="40" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonRS-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptLB">
        <Image Width="46" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonL1-XboxDark}" />
    </DataTemplate>

    <DataTemplate x:Key="ButtonPromptRB">
        <Image Width="46" Height="40" RenderOptions.BitmapScalingMode="Fant" Source="{DynamicResource ImageButtonR1-XboxDark}" />
    </DataTemplate>

    <BitmapImage x:Key="ImageButtonA-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-A.png'}" />
    <BitmapImage x:Key="ImageButtonB-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-B.png'}" />
    <BitmapImage x:Key="ImageButtonX-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-X.png'}" />
    <BitmapImage x:Key="ImageButtonY-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-Y.png'}" />
    <BitmapImage x:Key="ImageButtonStart-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-Start.png'}" />
    <BitmapImage x:Key="ImageButtonBack-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-Back.png'}" />
    <BitmapImage x:Key="ImageButtonLS-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-LS.png'}" />
    <BitmapImage x:Key="ImageButtonRS-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-RS.png'}" />
    <BitmapImage x:Key="ImageButtonDpad-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-Dpad.png'}" />
    <BitmapImage x:Key="ImageButtonL1-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-LeftShoulder.png'}" />
    <BitmapImage x:Key="ImageButtonR1-XboxDark" UriSource="{ThemeFile 'Images/Buttons/Controller/Xbox/Xbox Dark/Button-RightShoulder.png'}" />

</ResourceDictionary>
