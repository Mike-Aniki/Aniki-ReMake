<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:pc="clr-namespace:Playnite.Controls;assembly=Playnite"
                    xmlns:pbeh="clr-namespace:Playnite.Behaviors;assembly=Playnite"
                    mc:Ignorable="d">

    <Style x:Key="MediaGalleryGamesWindowStyle" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">

                    <ControlTemplate.Resources>

                        <CubicEase x:Key="EaseOutSoft" EasingMode="EaseOut"/>
                        <QuadraticEase x:Key="EaseOutFast" EasingMode="EaseOut"/>

                        <Storyboard x:Key="SB_MediaTileToNone">
                            <DoubleAnimation Storyboard.TargetName="FocusTransform"
                                             Storyboard.TargetProperty="Y"
                                             To="0"
                                             Duration="0:0:0.16"
                                             EasingFunction="{StaticResource EaseOutFast}"/>

                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                             Storyboard.TargetProperty="ScaleX"
                                             To="1"
                                             Duration="0:0:0.16"
                                             EasingFunction="{StaticResource EaseOutFast}"/>

                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                             Storyboard.TargetProperty="ScaleY"
                                             To="1"
                                             Duration="0:0:0.16"
                                             EasingFunction="{StaticResource EaseOutFast}"/>
                        </Storyboard>

                        <Storyboard x:Key="SB_MediaTileToFocus">
                            <DoubleAnimation Storyboard.TargetName="FocusTransform"
                                             Storyboard.TargetProperty="Y"
                                             To="-6"
                                             Duration="0:0:0.18"
                                             EasingFunction="{StaticResource EaseOutSoft}"/>

                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                             Storyboard.TargetProperty="ScaleX"
                                             To="1.035"
                                             Duration="0:0:0.18"
                                             EasingFunction="{StaticResource EaseOutSoft}"/>

                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                             Storyboard.TargetProperty="ScaleY"
                                             To="1.035"
                                             Duration="0:0:0.18"
                                             EasingFunction="{StaticResource EaseOutSoft}"/>
                        </Storyboard>

                        <Storyboard x:Key="SB_MediaTileToHover">
                            <DoubleAnimation Storyboard.TargetName="FocusTransform"
                                             Storyboard.TargetProperty="Y"
                                             To="-4"
                                             Duration="0:0:0.14"
                                             EasingFunction="{StaticResource EaseOutFast}"/>

                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                             Storyboard.TargetProperty="ScaleX"
                                             To="1.02"
                                             Duration="0:0:0.14"
                                             EasingFunction="{StaticResource EaseOutFast}"/>

                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                             Storyboard.TargetProperty="ScaleY"
                                             To="1.02"
                                             Duration="0:0:0.14"
                                             EasingFunction="{StaticResource EaseOutFast}"/>
                        </Storyboard>

                        <Storyboard x:Key="SB_MediaTileToFocusHover">
                            <DoubleAnimation Storyboard.TargetName="FocusTransform"
                                             Storyboard.TargetProperty="Y"
                                             To="-10"
                                             Duration="0:0:0.18"
                                             EasingFunction="{StaticResource EaseOutSoft}"/>

                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                             Storyboard.TargetProperty="ScaleX"
                                             To="1.045"
                                             Duration="0:0:0.18"
                                             EasingFunction="{StaticResource EaseOutSoft}"/>

                            <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                             Storyboard.TargetProperty="ScaleY"
                                             To="1.045"
                                             Duration="0:0:0.18"
                                             EasingFunction="{StaticResource EaseOutSoft}"/>
                        </Storyboard>

                        <ControlTemplate x:Key="MediaGalleryTileTemplate" TargetType="ListBoxItem">
                            <ButtonEx Command="{PluginSettings Plugin=AnikiHelper, Path=OpenScreenshotsForMediaGameCommand}"
                                      CommandParameter="{Binding}"
                                      Margin="0,0,34,40"
                                      Focusable="True"
                                      FocusVisualStyle="{x:Null}">

                                <ButtonEx.Style>
                                    <Style TargetType="ButtonEx">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ButtonEx">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ButtonEx.Style>

                                <Grid Margin="0,24,0,24">
                                    <Grid x:Name="TileRoot"
                                          RenderTransformOrigin="0.5,0.5"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center">

                                        <Grid.RenderTransform>
                                            <TransformGroup>
                                                <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                                                <TranslateTransform x:Name="FocusTransform" X="0" Y="0"/>
                                            </TransformGroup>
                                        </Grid.RenderTransform>

                                        <StackPanel HorizontalAlignment="Center"
                                                    VerticalAlignment="Top">

                                            <Grid x:Name="CoverFrame"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center">

                                                <Viewbox x:Name="CoverViewbox"
                                                         MaxWidth="460"
                                                         MaxHeight="310"
                                                         Stretch="Uniform"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center">

                                                    <Grid x:Name="CoverRoot"
                                                          Width="{Settings GridItemWidth}"
                                                          Height="{Settings GridItemHeight}"
                                                          ClipToBounds="False"
                                                          SnapsToDevicePixels="True">

                                                        <Border x:Name="FocusGlow"
                                                                CornerRadius="{DynamicResource Corner}"
                                                                Background="{DynamicResource GlowFocusBrush}"
                                                                Opacity="0"
                                                                Margin="-4"
                                                                CacheMode="BitmapCache">
                                                            <Border.Effect>
                                                                <BlurEffect Radius="12" RenderingBias="Performance"/>
                                                            </Border.Effect>
                                                        </Border>

                                                        <Grid x:Name="ShadowLayer"
                                                              IsHitTestVisible="False"
                                                              Opacity="0.55">
                                                            <Grid.RenderTransform>
                                                                <TranslateTransform X="1" Y="1"/>
                                                            </Grid.RenderTransform>

                                                            <Border CornerRadius="{DynamicResource Corner}"
                                                                    Background="#50000000"/>
                                                        </Grid>

                                                        <Border x:Name="Mask"
                                                                Background="#181A1E"
                                                                CornerRadius="{DynamicResource Corner}"/>

                                                        <Grid x:Name="CoverContent">
                                                            <Grid.OpacityMask>
                                                                <VisualBrush Visual="{Binding ElementName=Mask}"/>
                                                            </Grid.OpacityMask>

                                                            <Image Stretch="{Settings CoverArtStretch}"
                                                                   StretchDirection="Both"
                                                                   RenderOptions.BitmapScalingMode="Linear"
                                                                   SnapsToDevicePixels="True">
                                                                <Image.Source>
                                                                    <Binding Path="CoverPath" IsAsync="True"/>
                                                                </Image.Source>
                                                            </Image>

                                                            <Grid>
                                                                <Grid.Style>
                                                                    <Style TargetType="Grid">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding HasCover}" Value="False">
                                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Grid.Style>

                                                                <Border Background="#77000000"/>

                                                                <TextBlock Text="&#xE7FC;"
                                                                           FontFamily="{DynamicResource FontIcoFont}"
                                                                           Style="{DynamicResource TextBlockBaseStyle}"
                                                                           FontSize="48"
                                                                           Opacity="0.58"
                                                                           HorizontalAlignment="Center"
                                                                           VerticalAlignment="Center"/>
                                                            </Grid>



                                                            <Border x:Name="FocusedTitleOverlay"
                                                                    VerticalAlignment="Bottom"
                                                                    Height="58"
                                                                    CornerRadius="{DynamicResource Corner}"
                                                                    Opacity="0"
                                                                    IsHitTestVisible="False">

                                                                <Border.Background>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                        <GradientStop Color="#00000000" Offset="0"/>
                                                                        <GradientStop Color="#D8000000" Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Border.Background>

                                                                <TextBlock Text="{Binding GameName}"
                                                                           Style="{DynamicResource TextBlockBaseStyle}"
                                                                           FontSize="18"
                                                                           FontWeight="SemiBold"
                                                                           TextTrimming="CharacterEllipsis"
                                                                           HorizontalAlignment="Center"
                                                                           VerticalAlignment="Bottom"
                                                                           TextAlignment="Center"
                                                                           Margin="14,0,14,9"
                                                                           Effect="{DynamicResource SoftTextShadow}"/>
                                                            </Border>

                                                            <Border x:Name="DimGames"
                                                                    Background="Black"
                                                                    IsHitTestVisible="False"
                                                                    CornerRadius="{DynamicResource Corner}"
                                                                    Opacity="0"/>

                                                            <Border x:Name="FlashFocus"
                                                                    Opacity="0"
                                                                    CornerRadius="{DynamicResource Corner}"
                                                                    Tag="False"
                                                                    Visibility="Collapsed"
                                                                    IsHitTestVisible="False"/>

                                                            <Border x:Name="MainBorder"
                                                                    Opacity="0"
                                                                    BorderThickness="3"
                                                                    CornerRadius="{DynamicResource Corner}"
                                                                    Tag="False"
                                                                    Visibility="Collapsed"
                                                                    BorderBrush="{DynamicResource FocusGameBorderBrush}"
                                                                    IsHitTestVisible="False"/>
                                                        </Grid>
                                                    </Grid>
                                                </Viewbox>

                                                <Border x:Name="CaptureBadge"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Top"
                                                        Margin="10,10,0,0"
                                                        Padding="11,5"
                                                        CornerRadius="13"
                                                        Background="{DynamicResource BackgroundMenu}"
                                                        BorderBrush="{DynamicResource MenuBorderBrush}"
                                                        BorderThickness="1"
                                                        Panel.ZIndex="50"
                                                        IsHitTestVisible="False">

                                                    <TextBlock Text="{Binding MediaCount}"
                                                               Style="{DynamicResource TextBlockBaseStyle}"
                                                               FontSize="16"
                                                               FontWeight="SemiBold"
                                                               Opacity="0.95"/>
                                                </Border>

                                            </Grid>

                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </ButtonEx>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter Property="Tag" Value="True" TargetName="FlashFocus"/>
                                    <Setter Property="Tag" Value="True" TargetName="MainBorder"/>
                                    <Setter Property="Visibility" Value="Visible" TargetName="MainBorder"/>
                                    <Setter Property="Opacity" Value="1" TargetName="MainBorder"/>
                                    <Setter Property="Visibility" Value="Visible" TargetName="FlashFocus"/>
                                    <Setter TargetName="FocusGlow" Property="Opacity" Value="0.25"/>
                                    <Setter TargetName="DimGames" Property="Opacity" Value="0"/>
                                    <Setter TargetName="FocusedTitleOverlay" Property="Opacity" Value="1"/>
                                </Trigger>

                                <Trigger Property="IsKeyboardFocusWithin" Value="False">
                                    <Setter Property="Tag" Value="False" TargetName="FlashFocus"/>
                                    <Setter Property="Tag" Value="False" TargetName="MainBorder"/>
                                    <Setter Property="Visibility" Value="Collapsed" TargetName="MainBorder"/>
                                    <Setter Property="Visibility" Value="Collapsed" TargetName="FlashFocus"/>
                                    <Setter TargetName="FocusGlow" Property="Opacity" Value="0"/>
                                    <Setter TargetName="DimGames" Property="Opacity" Value="0.12"/>
                                    <Setter TargetName="FocusedTitleOverlay" Property="Opacity" Value="0"/>
                                </Trigger>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="DimGames" Property="Opacity" Value="0"/>
                                </Trigger>

                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{PluginSettings Plugin=AnikiHelper, Path=Options[EnableFocusFlashCover]}" Value="True"/>
                                        <Condition Binding="{PluginSettings Plugin=AnikiHelper, Path=Options[PerformanceMode]}" Value="False"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Style" TargetName="FlashFocus" Value="{DynamicResource AnimatedFlashFocus}"/>
                                </MultiDataTrigger>

                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsKeyboardFocusWithin" Value="True"/>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <MultiTrigger.EnterActions>
                                        <RemoveStoryboard BeginStoryboardName="BS_None"/>
                                        <RemoveStoryboard BeginStoryboardName="BS_Hover"/>
                                        <RemoveStoryboard BeginStoryboardName="BS_Focus"/>
                                        <BeginStoryboard x:Name="BS_FocusHover" Storyboard="{StaticResource SB_MediaTileToFocusHover}" HandoffBehavior="SnapshotAndReplace"/>
                                    </MultiTrigger.EnterActions>
                                </MultiTrigger>

                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsKeyboardFocusWithin" Value="True"/>
                                        <Condition Property="IsMouseOver" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <MultiTrigger.EnterActions>
                                        <RemoveStoryboard BeginStoryboardName="BS_None"/>
                                        <RemoveStoryboard BeginStoryboardName="BS_Hover"/>
                                        <RemoveStoryboard BeginStoryboardName="BS_FocusHover"/>
                                        <BeginStoryboard x:Name="BS_Focus" Storyboard="{StaticResource SB_MediaTileToFocus}" HandoffBehavior="SnapshotAndReplace"/>
                                    </MultiTrigger.EnterActions>
                                </MultiTrigger>

                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsKeyboardFocusWithin" Value="False"/>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <MultiTrigger.EnterActions>
                                        <RemoveStoryboard BeginStoryboardName="BS_None"/>
                                        <RemoveStoryboard BeginStoryboardName="BS_Focus"/>
                                        <RemoveStoryboard BeginStoryboardName="BS_FocusHover"/>
                                        <BeginStoryboard x:Name="BS_Hover" Storyboard="{StaticResource SB_MediaTileToHover}" HandoffBehavior="SnapshotAndReplace"/>
                                    </MultiTrigger.EnterActions>
                                </MultiTrigger>

                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsKeyboardFocusWithin" Value="False"/>
                                        <Condition Property="IsMouseOver" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <MultiTrigger.EnterActions>
                                        <RemoveStoryboard BeginStoryboardName="BS_Hover"/>
                                        <RemoveStoryboard BeginStoryboardName="BS_Focus"/>
                                        <RemoveStoryboard BeginStoryboardName="BS_FocusHover"/>
                                        <BeginStoryboard x:Name="BS_None" Storyboard="{StaticResource SB_MediaTileToNone}" HandoffBehavior="SnapshotAndReplace"/>
                                    </MultiTrigger.EnterActions>
                                </MultiTrigger>

                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{PluginSettings Plugin=AnikiHelper, Path=Options[PerformanceMode]}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="FocusGlow" Property="Opacity" Value="0"/>
                                </MultiDataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>

                        <Style x:Key="MediaGalleryTileContainerStyle" TargetType="ListBoxItem">
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="IsTabStop" Value="False"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Continue"/>
                            <Setter Property="Template" Value="{StaticResource MediaGalleryTileTemplate}"/>

                            <Style.Triggers>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter Property="IsSelected" Value="True"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <Style x:Key="MediaGalleryTileListStyle" TargetType="ListBox">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
                            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="False"/>
                            <Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Once"/>
                            <Setter Property="KeyboardNavigation.TabNavigation" Value="Once"/>
                            <Setter Property="KeyboardNavigation.ControlTabNavigation" Value="Once"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="ItemsPanel">
                                <Setter.Value>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"
                                                   HorizontalAlignment="Center"/>
                                    </ItemsPanelTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                    </ControlTemplate.Resources>

                    <Grid x:Name="RootGrid"
                          Background="{DynamicResource SecondaryViewBackground}"
                          KeyboardNavigation.DirectionalNavigation="Cycle"
                          KeyboardNavigation.TabNavigation="Cycle"
                          KeyboardNavigation.ControlTabNavigation="Cycle"
                          FocusManager.FocusedElement="{Binding ElementName=MediaGamesGrid}"
                          UseLayoutRounding="True"
                          SnapsToDevicePixels="True"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Opacity="0">

                        <Grid.RenderTransform>
                            <TranslateTransform Y="0"/>
                        </Grid.RenderTransform>

                        <Grid.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0"
                                                         To="1"
                                                         Duration="0:0:0.25"/>

                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                                         From="24"
                                                         To="0"
                                                         Duration="0:0:0.35">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Grid.Triggers>

                        <Grid IsHitTestVisible="False">
                            <Border Background="{DynamicResource SecondaryViewBackground}"/>

                            <Border Background="{DynamicResource WelcomeImage1}" Opacity="0.20">
                                <Border.OpacityMask>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#00FFFFFF" Offset="0"/>
                                        <GradientStop Color="#FFFFFFFF" Offset="0.28"/>
                                        <GradientStop Color="#FFFFFFFF" Offset="0.76"/>
                                        <GradientStop Color="#00FFFFFF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.OpacityMask>
                            </Border>

                            <Border Background="{DynamicResource ShadeBackgroundBrush}" Opacity="0.88"/>

                            <Border HorizontalAlignment="Left"
                                    Width="360">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#D8000000" Offset="0"/>
                                        <GradientStop Color="#00000000" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>

                            <Border VerticalAlignment="Bottom"
                                    Height="260">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                        <GradientStop Color="#BB000000" Offset="0"/>
                                        <GradientStop Color="#00000000" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Grid>

                        <Border HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Padding="38,26"
                                CornerRadius="26"
                                Background="#B0000000"
                                BorderBrush="#22FFFFFF"
                                BorderThickness="1"
                                Panel.ZIndex="20">

                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{PluginSettings Plugin=AnikiHelper, Path=MediaGalleryGames.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <StackPanel>
                                <TextBlock Text="{DynamicResource LOCMediaGalleryNoCacheTitle}"
                                           Style="{DynamicResource TextBlockBaseStyle}"
                                           FontSize="30"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"/>

                                <TextBlock Text="{DynamicResource LOCMediaGalleryPluginRequirement}"
                                           Style="{DynamicResource TextBlockDetailStyle}"
                                           FontSize="19"
                                           Opacity="0.72"
                                           Margin="0,10,0,0"
                                           HorizontalAlignment="Center"/>

                                <ButtonEx Margin="0,24,0,0"
                                          Height="52"
                                          MinWidth="230"
                                          Padding="18,0"
                                          HorizontalAlignment="Center"
                                          Command="{PluginSettings Plugin=AnikiHelper, Path=RefreshMediaGalleryLibraryCommand}"
                                          Focusable="True"
                                          FocusVisualStyle="{x:Null}">

                                    <ButtonEx.Style>
                                        <Style TargetType="ButtonEx" BasedOn="{StaticResource GameOptionScaleStyle}">
                                            <Setter Property="Background" Value="#33000000"/>
                                            <Setter Property="BorderBrush" Value="#22FFFFFF"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                        </Style>
                                    </ButtonEx.Style>

                                    <StackPanel Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center">

                                        <TextBlock Text="&#xE72C;"
                                                   FontFamily="{DynamicResource FontIcoFont}"
                                                   Style="{DynamicResource TextBlockBaseStyle}"
                                                   FontSize="18"
                                                   Opacity="0.78"
                                                   Margin="0,0,10,0"/>

                                        <TextBlock Text="{DynamicResource LOCMediaGalleryRefreshLibrary}"
                                                   Style="{DynamicResource TextBlockBaseStyle}"
                                                   FontSize="17"
                                                   FontWeight="SemiBold"
                                                   Opacity="0.84"/>
                                    </StackPanel>
                                </ButtonEx>
                            </StackPanel>
                        </Border>

                        <Grid>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="220"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="60"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0"
                                  Margin="50,35,50,0"
                                  VerticalAlignment="Top"
                                  Panel.ZIndex="5">

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0"
                                                Orientation="Vertical">

                                        <TextBlock Text="{DynamicResource LOCMediaGalleryTitle}"
                                                   Style="{DynamicResource TextBlockBaseStyle}"
                                                   FontSize="43"
                                                   FontWeight="SemiBold"
                                                   Effect="{DynamicResource SoftTextShadow}"/>

                                        <StackPanel Orientation="Horizontal"
                                                    Margin="0,10,0,0"
                                                    Effect="{DynamicResource SoftTextShadow}">

                                            <TextBlock Text="{PluginSettings Plugin=AnikiHelper, Path=MediaGalleryGames.Count}"
                                                       Style="{DynamicResource TextBlockDetailStyle}"
                                                       FontSize="18"
                                                       Opacity="0.76"/>

                                            <TextBlock Text="{DynamicResource LOCMediaGalleryGamesWithCaptures}"
                                                       Margin="5,0,0,0"
                                                       Style="{DynamicResource TextBlockDetailStyle}"
                                                       FontSize="18"
                                                       Opacity="0.76"/>

                                            <TextBlock Text="•"
                                                       Margin="14,0"
                                                       Style="{DynamicResource TextBlockDetailStyle}"
                                                       FontSize="18"
                                                       Opacity="0.42"/>

                                            <TextBlock Text="{PluginSettings Plugin=AnikiHelper, Path=MediaGalleryProviderName}"
                                                       Style="{DynamicResource TextBlockDetailStyle}"
                                                       FontSize="18"
                                                       Opacity="0.8"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <!--  <ButtonEx Grid.Column="1"
                                    Width="58"
                                    Height="58"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Command="{PluginSettings Plugin=AnikiHelper, Path=CloseTopWindowCommand}"
                                    FocusVisualStyle="{x:Null}">

                                    <ButtonEx.Style>
                                    <Style TargetType="ButtonEx" BasedOn="{StaticResource GameOptionScaleStyle}">
                                    <Setter Property="Background" Value="#33000000"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="BorderBrush" Value="#22FFFFFF"/>
                                    </Style>
                                    </ButtonEx.Style>

                                    <TextBlock Text="&#xE8BB;"
                                    FontFamily="{DynamicResource FontIcoFont}"
                                    Style="{DynamicResource TextBlockBaseStyle}"
                                    FontSize="23"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                                    </ButtonEx> -->

                                </Grid>

                                <Grid Grid.Row="1"
                                      Margin="0,34,0,0">

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <ComboBoxEx Grid.Column="0"
                                                Width="340"
                                                Height="52"
                                                SelectedValue="{PluginSettings Plugin=AnikiHelper, Path=MediaGalleryGamesSortMode, Mode=TwoWay}"
                                                SelectedValuePath="Tag"
                                                FocusVisualStyle="{x:Null}">

                                        <ComboBoxItem Content="{DynamicResource LOCMediaGallerySortNewest}"
                                                      Tag="LatestCaptureDesc"/>

                                        <ComboBoxItem Content="{DynamicResource LOCMediaGallerySortOldest}"
                                                      Tag="LatestCaptureAsc"/>

                                        <ComboBoxItem Content="{DynamicResource LOCMediaGallerySortMostCaptures}"
                                                      Tag="MediaCountDesc"/>

                                        <ComboBoxItem Content="{DynamicResource LOCMediaGallerySortFewestCaptures}"
                                                      Tag="MediaCountAsc"/>

                                        <ComboBoxItem Content="{DynamicResource LOCMediaGallerySortNameAsc}"
                                                      Tag="GameNameAsc"/>

                                        <ComboBoxItem Content="{DynamicResource LOCMediaGallerySortNameDesc}"
                                                      Tag="GameNameDesc"/>
                                    </ComboBoxEx>

                                    <!--      <ButtonEx Grid.Column="2"
                                    Height="52"
                                    MinWidth="230"
                                    Padding="18,0"
                                    HorizontalAlignment="Right"
                                    Command="{PluginSettings Plugin=AnikiHelper, Path=RefreshMediaGalleryLibraryCommand}"
                                    FocusVisualStyle="{x:Null}">

                                    <ButtonEx.Style>
                                    <Style TargetType="ButtonEx" BasedOn="{StaticResource GameOptionScaleStyle}">
                                    <Setter Property="Background" Value="#22000000"/>
                                    <Setter Property="BorderBrush" Value="#16FFFFFF"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    </Style>
                                    </ButtonEx.Style>

                                    <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center">

                                    <TextBlock Text="&#xE72C;"
                                    FontFamily="{DynamicResource FontIcoFont}"
                                    Style="{DynamicResource TextBlockBaseStyle}"
                                    FontSize="18"
                                    Opacity="0.78"
                                    Margin="0,0,10,0"/>

                                    <TextBlock Text="{DynamicResource LOCMediaGalleryRefreshLibrary}"
                                    Style="{DynamicResource TextBlockBaseStyle}"
                                    FontSize="17"
                                    FontWeight="SemiBold"
                                    Opacity="0.84"/>
                                    </StackPanel>
                                    </ButtonEx> -->

                                </Grid>
                            </Grid>

                            <ListBox x:Name="MediaGamesGrid"
                                     Grid.Row="1"
                                     Margin="0,10,0,0"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Stretch"
                                     Style="{StaticResource MediaGalleryTileListStyle}"
                                     ItemContainerStyle="{StaticResource MediaGalleryTileContainerStyle}"
                                     ItemsSource="{PluginSettings Plugin=AnikiHelper, Path=MediaGalleryGames}"
                                     SelectedIndex="0"
                                     SelectionMode="Single"
                                     pbeh:FocusBahaviors.FocusBinding="True"
                                     pbeh:FocusBahaviors.OnVisibilityFocus="True"
                                     Panel.ZIndex="2"/>



                            <Border Grid.Row="2"
                                    VerticalAlignment="Bottom"
                                    Height="60"
                                    Padding="26,0,26,0"
                                    Background="{DynamicResource BottomBar}"
                                    Panel.ZIndex="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                Opacity="0.75">

                                        <ButtonEx Style="{DynamicResource ButtonBottomMenuDark}"
                                                  Tag="B"
                                                  Content="{DynamicResource LOCBackLabel}" />

                                        <ButtonEx Style="{DynamicResource ButtonBottomMenuDark}"
                                                  Tag="A"
                                                  Content="{DynamicResource LOCButtonPrompt_Select}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                        </Grid>

                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <DataTrigger Binding="{PluginSettings Plugin=AnikiHelper, Path=AspectRatio}" Value="dsp1610">
                <Setter Property="Margin" Value="0,-60,0,-60"/>
            </DataTrigger>

            <DataTrigger Binding="{PluginSettings Plugin=AnikiHelper, Path=AspectRatio}" Value="dsp219">
                <Setter Property="Margin" Value="-320,0,-320,0"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>