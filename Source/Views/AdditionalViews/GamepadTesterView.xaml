<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:pc="clr-namespace:Playnite.Controls;assembly=Playnite"
                    xmlns:pbeh="clr-namespace:Playnite.Behaviors;assembly=Playnite"
                    mc:Ignorable="d">

    <!-- Gamepad Tester -->
    <Style x:Key="GamepadTesterWindowStyle" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">

                    <Grid x:Name="GamepadTesterRoot"
                          Background="{DynamicResource SecondaryViewBackground}"
                          FocusManager.IsFocusScope="True"
                          KeyboardNavigation.DirectionalNavigation="Contained"
                          KeyboardNavigation.TabNavigation="Cycle"
                          KeyboardNavigation.ControlTabNavigation="Contained"
                          Opacity="0">

                        <Grid.RenderTransform>
                            <TranslateTransform Y="0"/>
                        </Grid.RenderTransform>

                        <Grid.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0"
                                                         To="1"
                                                         Duration="0:0:0.22"/>

                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                                         From="24"
                                                         To="0"
                                                         Duration="0:0:0.32">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Grid.Triggers>

                        <Grid.Resources>

                            <Style x:Key="GamepadTesterTitleStyle" TargetType="TextBlock" BasedOn="{StaticResource TextBlockBaseStyle}">
                                <Setter Property="Foreground" Value="{DynamicResource TextHighlightBrush}"/>
                                <Setter Property="FontSize" Value="18"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Margin" Value="0,0,0,12"/>
                            </Style>

                            <Style x:Key="GamepadTesterSubtitleStyle" TargetType="TextBlock" BasedOn="{StaticResource TextBlockBaseStyle}">
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Opacity" Value="0.68"/>
                                <Setter Property="TextWrapping" Value="Wrap"/>
                            </Style>

                            <Style x:Key="GamepadTesterCardStyle" TargetType="Border">
                                <Setter Property="CornerRadius" Value="14"/>
                                <Setter Property="Padding" Value="18"/>
                                <Setter Property="Background" Value="{DynamicResource OverlayMenu}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="SnapsToDevicePixels" Value="True"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="10"
                                                          ShadowDepth="0"
                                                          Opacity="0.35"
                                                          Color="#000000"/>
                                    </Setter.Value>
                                </Setter>
                            </Style>

                            <Style x:Key="GamepadTesterBlockStyle" TargetType="ContentControl">
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="IsTabStop" Value="False"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Continue"/>
                                <Setter Property="KeyboardNavigation.TabNavigation" Value="Continue"/>
                                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                <Setter Property="VerticalAlignment" Value="Stretch"/>
                            </Style>

                            <Style x:Key="GamepadTesterInteractiveBlockStyle"
                                   TargetType="ContentControl"
                                   BasedOn="{StaticResource GamepadTesterBlockStyle}">
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="IsTabStop" Value="False"/>
                                <Setter Property="KeyboardNavigation.DirectionalNavigation" Value="Continue"/>
                                <Setter Property="KeyboardNavigation.TabNavigation" Value="Continue"/>
                            </Style>

                        </Grid.Resources>

                        <!-- Background shade -->
                        <Border Background="#88000000"/>

                        <Grid Margin="56,42,56,30">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Header -->
                            <Grid Grid.Row="0" Margin="0,0,0,26">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                    <TextBlock Text="Gamepad Tester"
                                               Style="{DynamicResource TextBlockBaseStyle}"
                                               Foreground="{DynamicResource TextHighlightBrush}"
                                               FontSize="34"
                                               FontWeight="Bold"
                                               TextTrimming="CharacterEllipsis"/>

                                    <TextBlock Text="Live controller diagnostics for buttons, sticks, vibration and latency."
                                               Style="{StaticResource GamepadTesterSubtitleStyle}"
                                               Margin="2,6,0,0"/>
                                </StackPanel>

                                <Border Grid.Column="1"
                                        Style="{StaticResource GamepadTesterCardStyle}"
                                        Padding="14,10"
                                        MinWidth="260"
                                        VerticalAlignment="Center">
                                    <ContentControl x:Name="GamepadTester_StatusBadge"
                                                    Tag="GamepadTester_StatusBadge"
                                                    Style="{StaticResource GamepadTesterBlockStyle}"
                                                    MinHeight="44"
                                                    Focusable="False"
                                                    IsTabStop="False"/>
                                </Border>
                            </Grid>

                            <!-- Main content -->
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.35*"/>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Controller and stick diagnostics -->
                                <Grid Grid.Column="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="1.35*"/>
                                        <RowDefinition Height="18"/>
                                        <RowDefinition Height="0.78*"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0" Style="{StaticResource GamepadTesterCardStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <StackPanel Orientation="Vertical" Grid.Row="0">
                                                <TextBlock Text="Button map" Style="{StaticResource GamepadTesterTitleStyle}"/>
                                                <TextBlock Text="Start the focused test, then press every control on the gamepad."
                                                           Style="{StaticResource GamepadTesterSubtitleStyle}"
                                                           Margin="0,-6,0,12"/>
                                            </StackPanel>

                                            <ContentControl x:Name="GamepadTester_ButtonMap"
                                                            Tag="GamepadTester_ButtonMap"
                                                            Grid.Row="1"
                                                            Style="{StaticResource GamepadTesterInteractiveBlockStyle}"/>
                                        </Grid>
                                    </Border>

                                    <Border Grid.Row="2" Style="{StaticResource GamepadTesterCardStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="Stick diagnostics" Style="{StaticResource GamepadTesterTitleStyle}"/>

                                            <ContentControl x:Name="GamepadTester_StickCheck"
                                                            Tag="GamepadTester_StickCheck"
                                                            Grid.Row="1"
                                                            Style="{StaticResource GamepadTesterBlockStyle}"/>
                                        </Grid>
                                    </Border>
                                </Grid>

                                <!-- Right column -->
                                <Grid Grid.Column="2"
                                      KeyboardNavigation.DirectionalNavigation="Continue"
                                      KeyboardNavigation.TabNavigation="Continue">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="14"/>
                                        <RowDefinition Height="0.72*"/>
                                        <RowDefinition Height="18"/>
                                        <RowDefinition Height="1.18*"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0" Style="{StaticResource GamepadTesterCardStyle}">
                                        <ContentControl x:Name="GamepadTester_TriggerCheck"
                                                        Tag="GamepadTester_TriggerCheck"
                                                        Style="{StaticResource GamepadTesterBlockStyle}"/>
                                    </Border>

                                    <Border Grid.Row="2" Style="{StaticResource GamepadTesterCardStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="Rumble" Style="{StaticResource GamepadTesterTitleStyle}"/>

                                            <ContentControl x:Name="GamepadTester_RumblePad"
                                                            Tag="GamepadTester_RumblePad"
                                                            Grid.Row="1"
                                                            Style="{StaticResource GamepadTesterInteractiveBlockStyle}"/>
                                        </Grid>
                                    </Border>

                                    <Border Grid.Row="4" Style="{StaticResource GamepadTesterCardStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="Latency" Style="{StaticResource GamepadTesterTitleStyle}"/>

                                            <ContentControl x:Name="GamepadTester_LatencyMini"
                                                            Tag="GamepadTester_LatencyMini"
                                                            Grid.Row="1"
                                                            Style="{StaticResource GamepadTesterInteractiveBlockStyle}"/>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Grid>

                            <!-- Footer -->
                            <Grid Grid.Row="2" Height="58" Margin="0,22,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="A: activate"
                                           Style="{DynamicResource TextBlockBaseStyle}"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Opacity="0.7"
                                           VerticalAlignment="Center"
                                           TextWrapping="Wrap"/>

                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">

                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Content.DataContext.IsButtonCaptureRunning, ElementName=GamepadTester_ButtonMap}"
                                                             Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Content.DataContext.IsStickCaptureRunning, ElementName=GamepadTester_StickCheck}"
                                                             Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Content.DataContext.IsLatencyTestRunning, ElementName=GamepadTester_LatencyMini}"
                                                             Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>

                                    <ButtonEx x:Name="GamepadTester_BackButton"
                                              Content="{DynamicResource LOCBackLabel}">
                                        <ButtonEx.Style>
                                            <Style TargetType="ButtonEx"
                                                   BasedOn="{StaticResource ButtonBottomMenuDark}">
                                                <Setter Property="Tag" Value="B"/>
                                                <Setter Property="Command" Value="{Binding CloseCommand}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Content.DataContext.IsButtonCaptureRunning, ElementName=GamepadTester_ButtonMap}"
                                                                 Value="True">
                                                        <Setter Property="Tag" Value="{x:Null}"/>
                                                        <Setter Property="Command" Value="{x:Null}"/>
                                                        <Setter Property="IsEnabled" Value="False"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Content.DataContext.IsStickCaptureRunning, ElementName=GamepadTester_StickCheck}"
                                                                 Value="True">
                                                        <Setter Property="Tag" Value="{x:Null}"/>
                                                        <Setter Property="Command" Value="{x:Null}"/>
                                                        <Setter Property="IsEnabled" Value="False"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Content.DataContext.IsLatencyTestRunning, ElementName=GamepadTester_LatencyMini}"
                                                                 Value="True">
                                                        <Setter Property="Tag" Value="{x:Null}"/>
                                                        <Setter Property="Command" Value="{x:Null}"/>
                                                        <Setter Property="IsEnabled" Value="False"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ButtonEx.Style>
                                    </ButtonEx>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Grid>

                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <DataTrigger Binding="{PluginSettings Plugin=AnikiHelper, Path=AspectRatio}" Value="dsp1610">
                <Setter Property="Margin" Value="0,-60,0,-60"/>
            </DataTrigger>

            <DataTrigger Binding="{PluginSettings Plugin=AnikiHelper, Path=AspectRatio}" Value="dsp219">
                <Setter Property="Margin" Value="-320,0,-320,0"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
